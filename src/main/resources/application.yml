spring:
  jpa:
    hibernate:
      ddl-auto: create
      use-new-id-generator-mappings: false
    show-sql: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/test?serverTimeZone=UTC&characterEncoding=UTF-8
    username: root
    password: 1111

jwt:
  secret: secretkey